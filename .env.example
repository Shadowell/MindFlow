# Copy to .env and fill the values your project needs.

APP_ENV=development

# Required only when running database-backed backend operations.
# Do not commit real production credentials.
# DATABASE_URL=postgresql+psycopg://mindflow:change-me@localhost:5432/mindflow
